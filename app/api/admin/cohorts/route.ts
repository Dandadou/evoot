import {NextResponse} from 'next/server';
import {getDB} from '@/lib/db';
const ORG='evolution-pme';
export async function GET(){try{const db=getDB();const [cohorts,trainings]=await Promise.all([db.prepare(`SELECT c.id,c.name,c.training_id trainingId,c.status,c.starts_at startsAt,c.ends_at endsAt,t.title trainingTitle,COUNT(cm.id) memberCount FROM cohorts c JOIN trainings t ON t.id=c.training_id LEFT JOIN cohort_members cm ON cm.cohort_id=c.id AND cm.role='LEARNER' AND cm.status='ACTIVE' WHERE c.organization_id=? GROUP BY c.id ORDER BY c.created_at DESC`).bind(ORG).all(),db.prepare(`SELECT id,title FROM trainings WHERE organization_id=? ORDER BY title`).bind(ORG).all()]);return NextResponse.json({cohorts:cohorts.results||[],trainings:trainings.results||[]})}catch(e){return NextResponse.json({error:String(e)},{status:500})}}
export async function POST(req:Request){try{const b=await req.json();const name=String(b.name||'').trim();const trainingId=Number(b.trainingId);if(!name||!trainingId)return NextResponse.json({error:'Nom et formation requis'},{status:400});const db=getDB();const training=await db.prepare('SELECT id FROM trainings WHERE id=? AND organization_id=?').bind(trainingId,ORG).first();if(!training)return NextResponse.json({error:'Formation introuvable'},{status:404});await db.prepare(`INSERT INTO cohorts (organization_id,training_id,name,status,starts_at,ends_at) VALUES (?,?,?,'ACTIVE',?,?)`).bind(ORG,trainingId,name,b.startsAt?String(b.startsAt):null,b.endsAt?String(b.endsAt):null).run();return NextResponse.json({ok:true},{status:201})}catch(e){return NextResponse.json({error:String(e)},{status:500})}}
export async function DELETE(req:Request){try{const b=await req.json().catch(()=>({}));const id=Number(b.id);if(!id)return NextResponse.json({error:'Groupe invalide'},{status:400});const db=getDB();const scheduled=await db.prepare(`SELECT COUNT(*) n FROM course_sessions WHERE organization_id=? AND cohort_id=? AND status NOT IN ('CANCELLED','COMPLETED')`).bind(ORG,id).first<{n:number}>();if((scheduled?.n||0)>0)return NextResponse.json({error:'Ce groupe a des séances actives ou planifiées. Supprimez ou terminez ces séances avant de supprimer le groupe.'},{status:409});await db.prepare('DELETE FROM cohorts WHERE id=? AND organization_id=?').bind(id,ORG).run();return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:String(e)},{status:500})}}
