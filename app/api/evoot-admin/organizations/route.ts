import {NextRequest,NextResponse} from 'next/server';
import {getDB} from '@/lib/db';
import {requireEvootAdmin} from '@/lib/auth';
import {ensureMasterAdmin} from '@/lib/master-access';

const HEX=/^#[0-9A-Fa-f]{6}$/;const EMAIL=/^[^\s@]+@[^\s@]+\.[^\s@]+$/;
const slugify=(value:string)=>value.normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'').slice(0,60);
const text=(value:unknown)=>typeof value==='string'?value.trim():'';
export async function GET(){try{await requireEvootAdmin();const db=getDB();const rows=await db.prepare(`SELECT o.id,o.name,o.type,o.status,o.timezone,o.locale,o.branding_json brandingJson,o.created_at createdAt,COUNT(DISTINCT om.user_id) members,(SELECT COUNT(*) FROM organization_master_access ma WHERE ma.organization_id=o.id AND ma.status='ACTIVE') masterEmails FROM organizations o LEFT JOIN organization_members om ON om.organization_id=o.id AND om.status='ACTIVE' WHERE o.id<>'evoot' GROUP BY o.id ORDER BY o.name`).all();return NextResponse.json({ok:true,organizations:rows.results||[]});}catch(error){if(error instanceof Error&&error.message==='UNAUTHORIZED')return NextResponse.json({error:'Non autorisé'},{status:401});return NextResponse.json({error:'Impossible de charger les organisations'},{status:500});}}
export async function POST(request:NextRequest){try{await requireEvootAdmin();const body=await request.json() as Record<string,unknown>;const name=text(body.name);const id=slugify(text(body.id)||name);if(!name||!id)return NextResponse.json({error:'Nom de l’organisation requis'},{status:400});const type=['BUSINESS','SCHOOL','ORGANIZATION'].includes(text(body.type))?text(body.type):'BUSINESS';const primaryColor=HEX.test(text(body.primaryColor))?text(body.primaryColor):'#F7941D';const secondaryColor=HEX.test(text(body.secondaryColor))?text(body.secondaryColor):'#111111';const branding={primaryColor,secondaryColor,logoUrl:text(body.logoUrl),showPoweredByEvoot:true,poweredByLabel:'ÉVOOT! — Propulsé par Évolution PME · Créateur de marques'};const masters=Array.isArray(body.masterEmails)?[...new Set(body.masterEmails.map(text).filter(Boolean).map(v=>v.toLowerCase()))]:[];if(masters.some(email=>!EMAIL.test(email)))return NextResponse.json({error:'Un des courriels maîtres est invalide'},{status:400});if(!masters.length)return NextResponse.json({error:'Ajoute au moins un courriel maître'},{status:400});const db=getDB();await db.prepare(`INSERT INTO organizations(id,name,type,status,timezone,locale,branding_json) VALUES(?,?,?,'ACTIVE',?,?,?)`).bind(id,name,type,text(body.timezone)||'America/Toronto',text(body.locale)||'fr-CA',JSON.stringify(branding)).run();try{await db.prepare(`INSERT INTO organization_client_profiles(organization_id,legal_name,contact_name,contact_title,contact_email,contact_phone,website,address_line1,address_line2,city,province_state,postal_code,country,neq,tax_gst_number,tax_qst_number,billing_email,billing_contact_name,billing_phone,billing_address_same,billing_address_line1,billing_address_line2,billing_city,billing_province_state,billing_postal_code,billing_country,internal_notes) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).bind(id,text(body.legalName)||name,text(body.contactName),text(body.contactTitle),text(body.contactEmail),text(body.contactPhone),text(body.website),text(body.addressLine1),text(body.addressLine2),text(body.city),text(body.provinceState),text(body.postalCode),text(body.country)||'Canada',text(body.neq),text(body.taxGstNumber),text(body.taxQstNumber),text(body.billingEmail),text(body.billingContactName),text(body.billingPhone),body.billingAddressSame===false?0:1,text(body.billingAddressLine1),text(body.billingAddressLine2),text(body.billingCity),text(body.billingProvinceState),text(body.billingPostalCode),text(body.billingCountry),text(body.internalNotes)).run();for(const email of masters){await db.prepare(`INSERT INTO organization_master_access(organization_id,email,status) VALUES(?,?,'ACTIVE')`).bind(id,email).run();await ensureMasterAdmin(db,id,email);}}catch(error){await db.prepare(`DELETE FROM organizations WHERE id=?`).bind(id).run();throw error;}return NextResponse.json({ok:true,id},{status:201});}catch(error){if(error instanceof Error&&error.message==='UNAUTHORIZED')return NextResponse.json({error:'Non autorisé'},{status:401});console.error('Create organization failed',error);return NextResponse.json({error:'Impossible de créer l’organisation. Vérifie les informations et l’identifiant.'},{status:500});}}
export async function DELETE(request:NextRequest){try{await requireEvootAdmin();const body=await request.json() as {id?:string};const id=body.id?.trim();if(!id)return NextResponse.json({error:'Organisation requise'},{status:400});if(id==='evoot')return NextResponse.json({error:'L’organisation système EVOOT ne peut pas être supprimée'},{status:403});const db=getDB();const org=await db.prepare(`SELECT id,name FROM organizations WHERE id=?`).bind(id).first<{id:string;name:string}>();if(!org)return NextResponse.json({error:'Organisation introuvable'},{status:404});await db.prepare(`DELETE FROM organizations WHERE id=?`).bind(id).run();return NextResponse.json({ok:true,id,name:org.name});}catch(error){if(error instanceof Error&&error.message==='UNAUTHORIZED')return NextResponse.json({error:'Non autorisé'},{status:401});console.error('Delete organization failed',error);return NextResponse.json({error:'Impossible de supprimer l’organisation'},{status:500});}}
