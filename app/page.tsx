'use client';
import Link from 'next/link';
import { useRouter } from 'next/navigation';
import { useState } from 'react';
import './join-code.css';

export default function Home(){
 const[code,setCode]=useState('');const router=useRouter();
 const digits=(code+'      ').slice(0,6).split('');
 return <main className="shell"><nav className="nav"><div className="brand">ÉV<span>OO</span>T!</div><div className="homeNavActions"><a className="ghost" href="#join">Rejoindre avec un code</a><Link className="ghost" href="/login">Connexion</Link></div></nav><section className="hero"><div className="eyebrow">CRÉER · FORMER · INTERAGIR</div><h1>Une plateforme.<br/>Toute la formation.<br/><em>Ensemble.</em></h1><p className="lead">Créez vos formations, programmez vos cours, animez vos groupes et faites participer tout le monde. Et oui : le côté « Kahoot » reste bien vivant. 😄</p><div className="actions"><a className="primary" href="#join">J’ai un code de séance</a><Link className="secondary" href="/login">Accéder à ÉVOOT</Link></div></section><section id="join" className="join joinRedesign"><div className="joinIntro"><div className="eyebrow">ÉVOOT LIVE</div><h2>Le mode participation instantanée.</h2><p>Le formateur affiche un code. Les participants répondent sur leur téléphone. Aucun compte requis.</p></div><form className="joinForm joinCodeForm" onSubmit={e=>{e.preventDefault();if(code.length===6)router.push(`/join?code=${code}`)}}><label className="joinCodeLabel" htmlFor="session-code"><span>ENTRE LE CODE À 6 CHIFFRES</span></label><div className="joinCodeBox"><div className="digitPreview" aria-hidden="true">{digits.map((d,i)=><span key={i}>{d.trim()?d:'0'}</span>)}</div><input id="session-code" className="joinCodeInput" aria-label="Code de séance à 6 chiffres" inputMode="numeric" autoComplete="one-time-code" maxLength={6} value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,'').slice(0,6))}/></div><div className="joinCodeHint">ⓘ <span>Le code est composé de 6 chiffres.</span></div><button className="primary joinCodeButton" disabled={code.length!==6}>Rejoindre</button></form></section><section id="portals" className="portalChooser"><div className="eyebrow">ESPACES ÉVOOT</div><h2>Une seule connexion. Le bon portail.</h2><p className="portalChooserLead">Entre ton courriel, reçois ton code à 6 chiffres et EVOOT t’amène automatiquement vers ton espace selon ton rôle.</p><div className="portalChooserGrid portalChooserGridThree"><Link href="/login"><b>Administration</b><span>Formations, groupes, agenda, utilisateurs et suivi de l’organisation.</span></Link><Link href="/login"><b>Formateur</b><span>Séances, Régie, classe, outils pédagogiques et résultats.</span></Link><Link href="/login"><b>Apprenant</b><span>Cours, progression, ressources, travaux et rediffusions.</span></Link></div></section><section className="features"><article><strong>01</strong><h3>Créer</h3><p>Formations complètes, scénarios, médias, activités et ÉVOOT AI.</p></article><article><strong>02</strong><h3>Former</h3><p>Présentiel, virtuel, hybride, autonome et cours programmés.</p></article><article><strong>03</strong><h3>Interagir</h3><p>Questions Live, réponses instantanées, discussions, mains levées et ressources.</p></article></section><footer>ÉVOOT! — CRÉER · FORMER · INTERAGIR</footer><Link href="/login" className="platformAdminCorner" aria-label="Administration ÉVOOT" title="Administration ÉVOOT">⚙</Link></main>}
